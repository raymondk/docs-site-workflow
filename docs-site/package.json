{
  "name": "@dfinity/icp-cli-docs",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "prepare-docs": "../scripts/prepare-docs.sh ../docs .docs-temp",
    "dev": "npm run clean && npm run prepare-docs && astro dev",
    "build": "npm run prepare-docs && astro build",
    "preview": "astro preview",
    "clean": "rm -rf .docs-temp dist .astro"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/starlight": "^0.37.3",
    "astro": "^5.6.1",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "rehype-external-links": "^3.0.0",
    "typescript": "^5.7.3"
  }
}
